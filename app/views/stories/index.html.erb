<%= link_to 'Add story', new_story_path, class:" btn btn-primary k-story-btn" %>
<% @stories.each do |story| %>
<div class="k-background">
  <div class="k-wrapper">
      <p><%= link_to story.title, story_path(story) %></p>
      <p><%= story.user.full_name %></p>
  </div>
</div>
<%end%>
